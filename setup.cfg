[metadata]
name = experimentdb
description = Experiment Database

[options]
package_dir =
    = src
packages = find:
python_requires = >3
install_requires =
    jsonschema
    sqlalchemy
    pyyaml

[options.packages.find]
where = src
